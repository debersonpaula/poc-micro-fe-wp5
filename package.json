{
  "name": "poc-micro-fe-wp5-root",
  "private": true,
  "workspaces": [
    "app-main",
    "mod-app",
    "mod-header",
    "mod-home",
    "library"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
